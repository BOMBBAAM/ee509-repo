%% Golden Section
function [x_min,f_min,IFLAG,IFunc] = golden(x_0,epsilon,itmax)
    x_min = zeros(itmax,1);
    f_min = zeros(itmax,1);
    % IFLAG 0 if success, -999 otherwise
    IFLAG = -999; % initial condition
    IFunc
end